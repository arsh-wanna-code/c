#include <stdio.h>
 main() {
    int i = 6; // Initialize the counter

    do {
        printf("%d\n", i); // Print the current value of i
        i++; // Increment the counter
    } while (i <= 5);
 }